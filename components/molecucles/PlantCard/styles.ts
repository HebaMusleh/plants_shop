import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    wrapper:{
        borderRadius:6,
        flexBasis: "48%",
    marginBottom: 15,
    marginHorizontal: "1%",
    },
    textWrapper:{
        paddingTop:10
    }
})