import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    borderRadius: 6,
    marginBottom: 15,
    marginHorizontal: "1.5%",
    flexBasis:"auto"
  },
  textWrapper: {
    paddingTop: 10,
  },
});
